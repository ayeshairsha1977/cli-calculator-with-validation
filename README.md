# cli-calculator-with-validation
A CLI-based calculator built in Python with proper input validation and error handling.  Unlike basic calculators, this program prevents crashes by handling invalid inputs  and ensures a smooth user experience.
